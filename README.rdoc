== README

Fiftypercent is a Rails app designed to collect data about diversity ratios of speakers at events. 

* See the Gemfile for system dependencies

* The app uses a sqlite3 database. Run 'rake db:create' to create the database then run rake db:migrate' to create the tables.

* To run tests use 'rake test'

